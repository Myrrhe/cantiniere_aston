import { UserListSearchPipe } from './user-list-search.pipe';

describe('UserListSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UserListSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
